<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.master.query.mapper.ItemQueryMapper">

    <resultMap id="ItemMap" type="com.team2.master.entity.Item">
        <result property="itemId"        column="item_id"/>
        <result property="itemCode"      column="item_code"/>
        <result property="itemName"      column="item_name"/>
        <result property="itemNameKr"    column="item_name_kr"/>
        <result property="itemSpec"      column="item_spec"/>
        <result property="itemUnit"      column="item_unit"/>
        <result property="itemPackUnit"  column="item_pack_unit"/>
        <result property="itemUnitPrice" column="item_unit_price"/>
        <result property="itemWeight"    column="item_weight"/>
        <result property="itemHsCode"    column="item_hs_code"/>
        <result property="itemCategory"  column="item_category"/>
        <result property="itemStatus"    column="item_status"/>
        <result property="itemRegDate"   column="item_reg_date"/>
        <result property="createdAt"     column="created_at"/>
        <result property="updatedAt"     column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="ItemMap">
        SELECT item_id, item_code, item_name, item_name_kr, item_spec, item_unit,
               item_pack_unit, item_unit_price, item_weight, item_hs_code,
               item_category, item_status, item_reg_date, created_at, updated_at
        FROM items
        WHERE item_id = #{itemId}
    </select>

    <select id="findAll" resultMap="ItemMap">
        SELECT item_id, item_code, item_name, item_name_kr, item_spec, item_unit,
               item_pack_unit, item_unit_price, item_weight, item_hs_code,
               item_category, item_status, item_reg_date, created_at, updated_at
        FROM items
    </select>

    <select id="findByItemStatus" resultMap="ItemMap">
        SELECT item_id, item_code, item_name, item_name_kr, item_spec, item_unit,
               item_pack_unit, item_unit_price, item_weight, item_hs_code,
               item_category, item_status, item_reg_date, created_at, updated_at
        FROM items
        WHERE item_status = #{itemStatus}
    </select>

</mapper>
