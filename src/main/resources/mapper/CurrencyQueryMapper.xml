<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.master.mapper.CurrencyQueryMapper">

    <resultMap id="CurrencyMap" type="com.team2.master.entity.Currency">
        <result property="currencyId"     column="currency_id"/>
        <result property="currencyCode"   column="currency_code"/>
        <result property="currencyName"   column="currency_name"/>
        <result property="currencySymbol" column="currency_symbol"/>
    </resultMap>

    <select id="findById" resultMap="CurrencyMap">
        SELECT currency_id, currency_code, currency_name, currency_symbol
        FROM currencies
        WHERE currency_id = #{currencyId}
    </select>

    <select id="findAll" resultMap="CurrencyMap">
        SELECT currency_id, currency_code, currency_name, currency_symbol
        FROM currencies
    </select>

</mapper>
